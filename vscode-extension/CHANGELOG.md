# Change Log

## 2.0.4

- Initial release of the VS Code / Cursor extension.
- Auto-downloads the platform-specific `windsurfpoolapi` binary from the v2.0.4 GitHub release.
- Commands: start / stop / restart proxy, open dashboard, setup language server, copy endpoint.
- Status bar indicator shows running state and port.
- Auto-detect Windsurf Language Server across macOS / Linux / Windows standard install paths.
